Connector:
    name: compute_v1
    schema: googleCloud
    default_scope: 
        - cdc-shared-data-ai-uat
    update_field: UpdateDate
    
Models:

    ComputeZones:
        client_name: ZonesClient
        request_builder: ListZonesRequest
        query_name: list
        accepted_inputs:
            - project
        index_keys:
            - id
            - name

    ComputeInstances:
        client_name: InstancesClient
        request_builder: ListInstancesRequest
        query_name: list
        accepted_inputs:
            - project
            - zone
        index_keys:
            - id
            - name